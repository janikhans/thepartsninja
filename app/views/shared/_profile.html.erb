<div id="sidebar-profile" class="card">
  <% if current_user %>
    <div class="card-header text-center">
      <div class="profile-image">
        <%= gravatar_for(current_user) %>
      </div>
      <strong><%= current_user.username %></strong>
      <p>Welcome Back!</p>
    </div>
    <div class="card-body">
      <%= link_to "Dashboard", account_root_path, class:"btn-shadow btn-shadow-primary" %>
      <%= link_to "Profile", account_root_path, class:"btn-shadow btn-shadow-primary" %>
    </div>
  <% else %>
    <div class="profile-header"></div>
    <div class="profile-body text-center">
      <div class="profile-image">
        <%= gravatar_for(current_user) %>
      </div>
      <div class="profile-username">
        <h4>Hey You!</h4>
        <p>Yeah <strong>YOU!</strong> You're currently viewing Parts Web results as a guest. Did you know you'll get access to even more awesome-ness if you sign up?</p>
      </div>
      <div class="">
        <%= link_to "Sign Up", sign_up_path, class:"btn btn-default text-center" %>
        <%= link_to "Sign In", login__path, class:"btn btn-default text-center" %>
      </div>
    </div>
  <% end %>
</div>
