<h1 style="text-transform: uppercase; font-weight: 800;"><%= pluralize(search_record.vehicles.first.count_above_threshold, 'compatible model') %> found</h1>
<hr>
<ul class="list-unstyled">
  <li><strong>Vehicle:</strong> <%= search_record.vehicle.to_label %></li>
  <li><strong>Part:</strong> <%= search_record.fitment_note.name + " -" if search_record.fitment_note %> <%= search_record.category.name %></li>
  <li><strong>Pages:</strong> <%= search_record.total_pages %></li>
  <li><strong>Models:</strong> <%= search_record.grouped_count %></li>
  <li><strong>Max Score:</strong> <%= search_record.vehicles.first.max_score %></li>
  <li><strong>Threshold:</strong> <%= search_record.vehicles.first.count_above_threshold %>/<%= search_record.results_count %></li>
</ul>
