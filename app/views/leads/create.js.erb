<% if @lead.errors.any? %>
  $('#emailForm').html("<%= escape_javascript(render partial: 'leads/form') %>");
<% else %>
  $('#emailForm').html('<h1 class="navy text-center">Thanks!</h1><p class="text-center">We will keep you in the loop and let you know when the Sensei has opened the dojo!</p>');
<% end %>
