<%= simple_form_for([:admin, @user_profile]) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :bio %>
    <%= f.input :location %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "Update", class: "btn-primary" %>
  </div>
<% end %>
