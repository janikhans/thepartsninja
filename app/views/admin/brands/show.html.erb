<% provide(:header, "#{@brand.name}") %>

<%= @brand.name %> <%= @brand.website %>

<strong>Edit:</strong> <%= link_to 'Edit', edit_admin_brand_path(@brand) %>
<strong>Delete:</strong> <%= link_to 'Delete', admin_brand_path(@brand), method: :delete, data: {confirm: "Are you sure"} %>
