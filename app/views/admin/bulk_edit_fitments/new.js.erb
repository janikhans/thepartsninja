if ( !$('#new_bulk_edit_fitments').length ) {
  $('body').append('<%= j(render "modal_new") %>');
} else {
  $('#new_bulk_edit_fitments').replaceWith('<%= j(render "modal_new") %>');
};
$('#new_bulk_edit_fitments').modal();
