
  <div class="row m-b-lg animated fadeInDown">
    <div class="col-md-5">
        <div class="">
          <%= image_tag("missing-image.png", class:"img-responsive") %>
        </div>
    </div>
      <div class="col-md-7">
      <div class="">
            <table class="table small m-b-xs">
              <tbody>
                <tr>
                    <td><h3><strong>Brand:</strong> <%= link_to @part.product.brand.name, @part.product.brand.name %> </h3></td>
                    <td><h3><strong>Product:</strong> <%= @part.product.name %></h3></td>
                </tr>
                <tr>
                    <td><h3><strong>Part Number:</strong> <%= @part.part_number %></h3></td>
                    <td><h3><strong>Category:</strong> <%= @part.product.category.name %></h3></td>
                </tr>
                <tr><td></td><td></td></tr>
              </tbody>
            </table>
          </div>
          <div>
              <h4>Description</h4>
              <small>
                  <%= @part.product.description %> The FMF Gnarly pipe focuses on increasing the lower RPM horsepower and torque to get you out of the toughest terrain. When looking to add 4-stroke tractor like torque to your 2-stroke and you need a pipe to withstand the harsh elements, this is the pipe for you. Strong and rugged, this aggressive pipe dishes out handfuls of torque and bottom to mid range pull needed for late model 200cc through 500cc machines and the rigors of the off road domain.
              </small>
              <h4>Additional Notes</h4>
              <small>
                  <%= @part.note %>
              </small>
          </div>
      </div>
  </div>
  <div class="row">
      <div class="col-lg-12  animated fadeInRight">
        <div class="ibox">
            <div class="ibox-title">
                <h5>The <%= @part.product.brand.name %> <%= @part.product.name %> was designed for these models</h5>
                <div class="ibox-tools">
                    <a class="collapse-link">
                        <i class="fa fa-chevron-up"></i>
                    </a>
                </div>
            </div>
            <div class="ibox-content">
              <% if @part.oem_vehicles.any? %>
              <div class="row">
                <% @part.oem_vehicles.each do |v|%>
                  <div class="col-md-3 col-xs-6 oem-vehicle">
                   <%= link_to "#{v.to_label}", vehicle_path(v) %>
                  </div>
                <% end %>
              </div>
              <% else %>
                <div class="search-box-padding">
                    <h5 class="text-center">Search OEM Parts</h5>
                    <form class="search-container text-center" action="/search">
                      <input id="search-box" type="text" class="search-box" name="q" />
                      <label for="search-box"><span class="glyphicon glyphicon-search search-icon"></span></label>
                      <input type="submit" id="search-submit" />
                    </form>
                  </div>
                This part doesn't fit anything yet, would you like to add one? <%= link_to "Yes!", new_fitment_path  %>
              <% end %>
            </div>
        </div>

        <div class="ibox">
            <div class="ibox-title">
                <h5>The <%= @part.product.brand.name %> <%= @part.product.name %> can be replaced with these parts</h5>
                <div class="ibox-tools">
                    <a class="collapse-link">
                        <i class="fa fa-chevron-up"></i>
                    </a>
                </div>
            </div>
            <div class="ibox-content no-padding">
            <% if @compatibles.any?  %>
              <%= render partial: 'compatibles/compatible', collection: @compatibles %>
            <% else %>
              <div class="search-box-padding">
                <h5 class="text-center">Search Compatible Parts</h5>
                <form class="search-container text-center" action="/search">
                  <input id="search-box" type="text" class="search-box" name="q" />
                  <label for="search-box"><span class="glyphicon glyphicon-search search-icon"></span></label>
                  <input type="submit" id="search-submit" />
                </form>
              </div>
            <% end %>
          </div>
      </div>

      <div class="ibox">
          <div class="ibox-title">
            <h5>The <%= @part.product.brand.name %> <%= @part.product.name %> can potentially be replaced with these parts</h5>
              <div class="ibox-tools">
                <a class="collapse-link">
                  <i class="fa fa-chevron-up"></i>
                </a>
              </div>
          </div>
          <div class="ibox-content no-padding">
            <div class="search-box-padding">
              <h5 class="text-center">Search For Potential Parts</h5>
              <form class="search-container text-center" action="/search">
                <input id="search-box" type="text" class="search-box" name="q" />
                <label for="search-box"><span class="glyphicon glyphicon-search search-icon"></span></label>
                <input type="submit" id="search-submit" />
              </form>
            </div>
        </div>
      </div>

    </div>
  </div>
