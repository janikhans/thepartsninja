<%= simple_form_for(@discovery) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :user %>
    <%= f.input :comment %>
    <%= f.input :modifications %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
